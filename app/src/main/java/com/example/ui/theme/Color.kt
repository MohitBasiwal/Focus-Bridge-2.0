package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Frosted Glass (Glassmorphism) Theme Palette
val CosmicDark = Color(0xFF0F172A)       // Slate 900 primary background canvas
val DarkSlate = Color(0xFF1E293B)        // Slate 800 solid base surface
val GlassBase = Color(0x0FFFFFFF)        // White translucent glass fill (5% opacity)
val GlassBorder = Color(0x1BFFFFFF)      // Soft white sheen border (10% opacity)

// Atmospheric Aurora Neon Glowing Accents
val NeonViolet = Color(0xFF6366F1)       // Indigo 500 (Primary Brand Indicator)
val AuroraCyan = Color(0xFF8B5CF6)       // Violet 500 (Secondary Accent Indicator)
val AuroraPink = Color(0xFFFB7185)       // Rose 400 (Tertiary Warning/Highlight Indicator)
val GlassMint = Color(0xFF10B981)        // Emerald 500 (Active State / Success Accent)

// Text & Label Contrasts
val PureWhite = Color(0xFFFFFFFF)
val TextSlateLight = Color(0xFFF1F5F9)   // Slate 100 (High contrast readable text)
val TextSlateMuted = Color(0xFF94A3B8)   // Slate 400 (Secondary descriptive labels)
val TextSlateDark = Color(0xFF475569)    // Slate 600 (Disabled indicators or shadows)

